{
  "name": "aws-discord-lstep",
  "version": "1.0.0",
  "private": true,
  "description": "SnsClub Discord×Lステップ お客様向けフロー（AWSサーバーレス）",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "sam build",
    "deploy": "sam deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.658.0",
    "@aws-sdk/lib-dynamodb": "^3.658.0",
    "@aws-sdk/client-lambda": "^3.658.0",
    "@aws-sdk/client-s3": "^3.658.0"
  }
}
